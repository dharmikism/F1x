// Change this only when you want a different default. You can also set the
// URL from the extension Options page after loading the unpacked extension.
window.FIXONCE_DEFAULT_API = "http://localhost:8000";

